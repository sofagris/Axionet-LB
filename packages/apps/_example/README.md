# Example app package

Minimal reference package for **axionet.app/v1**. Used to freeze and CI-validate the App Store layout.

See [ADR-app-package-contract.md](../../../docs/ADR-app-package-contract.md).

Not a deployable runtime service.
